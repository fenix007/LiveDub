// Урезанная копия yandex-cloud/cloudapi yandex/cloud/ai/stt/v3/stt_service.proto:
// только потоковое распознавание, без HTTP-аннотаций и асинхронного API.
syntax = "proto3";

package speechkit.stt.v3;

import "speechkit/stt/v3/stt.proto";

service Recognizer {
  // Expects audio in real-time
  rpc RecognizeStreaming(stream StreamingRequest) returns (stream StreamingResponse);
}
